# PhotoCompress
PhotoCompress is a local Android image compression tool designed for batch processing photos within device directories. It supports compression, automatic backups, metadata preservation, and skipping already-processed files, all while ensuring absolute privacy with zero network uploads.
